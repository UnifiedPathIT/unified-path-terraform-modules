# Modules

Each module in development lives in its own subdirectory here: `modules/<module-name>/`. See the root [README](../README.md) and [`docs/registry-publishing.md`](../docs/registry-publishing.md) for the standard structure and the path to a public Terraform Registry listing.

No modules yet — this is the starting scaffold.
